{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:clientssrDev": "vite build --mode development --outDir ../ssr-api/dist/clientssr --emptyOutDir",
    "build:serverssrDev": "vite build --mode development --ssr src/main-ssr-server.js --outDir ../ssr-api/dist/serverssr --emptyOutDir",
    "build:ssrDev": "npm run build:clientssrDev && npm run build:serverssrDev"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vuex": "^4.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.1"
  }
}
